# Transcript: https://www.youtube.com/watch?v=mPrM6lnd5wU
# Source: youtube-captions
[Music] my name is conad hoffmire today I'm going to talk about local first apps using postrace logical replication and specifically how we built a high scaleability sync Engine with Dynamic partial replication to facilitate a local first app architecture so I'm going to give a bit of background uh to this project and then uh walk through some of the system design architecture and implementation details and then also spoke about how we worked with post logical replication and what some of the specific challenges are that we had to solve there um so first of all just starting with what local first architecture is so you may have heard about this um it has some significant overlap with offline first which is an idea that has been around for the last 10 plus years uh but local first is an architecture where app code Works directly with a client side in beded database which then automatically syncs with a backend database in the background so reads and writes go to the local database first and um this has a significant benefit for both app developers as well as end users so for app developers because you're working with a local database you don't have to deal with retrieving data via API and dealing with um caching of API calls and things like that and therefore it also simplifies the backend uh because the complexity of the API that you have to develop is reduced significantly because the data transfer basically gets taken care of by um the sync engine and then related to that you also get lower backend compute load dependency and cost and then for the end user because you're reading and writing to a local database that's on the user's device um it's near zero latency so you basically get an instantly reactive ux um and for the same reason the apps are uh because you're working with the local database the apps are always available so you're not dependent on a network API uh you read and write to the local database so they can actually work offline as well and then by virtue of using a sync engine uh as a side effect you can also get um real-time multi-user collaboration so I think these are really good reasons why local first architecture is is getting more and more uh traction um so when building this sync engine um as far as why we chose to focus on post chis uh for the past 10 plus years our company Journey apps has been our first product is was a full stack uh platform for offline first industrial apps and uh we were thinking of spinning off our um Standalone or our sync engine as a standalone product which came to be called Power sync and um you know we were quite familiar with some of the no seel based sync engines that are on the market so for example uh mongodb and Atlas device sync and realm and then couch TB pouch TB uh but sort of late 2022 when we started seriously looking into this uh it didn't um seem like there was really sort of a mature um equivalent sync engine for postris SQL that can also sync to a client side embedded um SQL based database um such as sqlite um and there's Al also other reasons why we decided to focus on post SQL light um we are uh big fans of SQL and of course it's as a language remains more Universal and it's very effective for advanced queries and then sqlite is hard to to beat uh when it comes to Performance flexibility and advanced functionality and also just how battle tested it is so um the famous statistic is that there's a trillion sqlite databases in active use it's of course in every smartphone and a lot of other places our architecture does allow for swapping out um the sqlite database uh with another client side database but initially we thought it would be best to start by focusing on on sqlite so I thought it would be helpful to to sort of see how we worked with postrace and built this around postrace so going to walk through the design architecture and implementation um of The Spar sync sync engine that we built uh and these are some of the things I'm going to focus on to start with design objectives and high level architecture so uh one of our main design objectives was to treat um the post Chris database as sacran so a big thing there was to work with standard postgress databases in a non-invasive way so require minimal to no changes to the schema the configuration and then related to that uh we didn't want to bypass the developers existing business logic authorization and validation we figured that you know if we're adopting this kind of sync engine um developers may be averse to it if we're writing directly back into their post database so we wanted to avoid that in our architecture Dynamic partial replication was a really important capability um you know the that's kind of a lynchpin capability of these kind of sync engines it's not very useful if you have a large postgress database in the back end but you can't actually effectively sync a subset of the data to the client side um and then low maintenance uh we wanted the system to be very low maintenance um especially when it comes to schema Evolution and then also provide providing strong consistency guarantees so early on in the architecture uh we made a big decision which is to separate the read or download path from the right and upload path so um for the read path we built middleware service which is called the baring service which interfaces with postris logical replication and then does the dynamic uh partial replication of the data to the clients but then for the right path we decided to make all the rights go through the developers uh existing backend so that they can utilize their existing um business logic and validations and things like that so um zooming in a bit on the the read path Dynamic partial replication so of course the objective there is to get a relevant subset of the data to users and um if you sort of just start uh first thinking about it you know a naive approach might be to allow the clients to make sort of arbitrary database queries maybe within some sort of permission policy framework and attempt to keep their result sets in sync um but uh if you think about the this kind of scenario where you have a local first app architect you can have you know thousands or millions of users that can go online and offline at any time they can come back online you know hours later days later or even weeks later and um that then you would basically have to sync the Deltas for their specific queries relative to their specific current sync State and you can multiply that by you know millions of users potentially or tens of thousands of users so that quickly becomes um problematic for high scalability so the design that we went with was to allow for predefining buckets of data and then buckets of data can be sh shared between users were applicable and then efficiently tracking changes to data in those buckets so whenever we detect a change in the post Christ database Through The Logical replication stream we basically figure out which buckets are affected by that change and then we update the operation history for that particular buckets um and then how it actually works is the developer that uses this par sync sync system they start by defining declarative sync rules with a single syntax where they group data into buckets so the buckets can reference parameters from the client so you can see there that they can access uh parameters from the JWT authentication payload um they can do a parameter query or they can retrieve additional parameters that are needed from elsewhere in the postris database and then they have their data queries where they query the actual data that is included for that particular bucket and then of course they can ident they can Define multiple of these buckets and then what the system does is based on those sync rules that have been defined it replicates and pre-processes data from postgis we perform the replication initially by taking a snapshot of the tables defined in the synchr with regular SQL queries and then keep update them incrementally with logical replication and then we have our own uh checkpoint system that keeps track of postgress lsns in the replication stream which I'll mention more about later and there's a lot of anal there's a lot of sort of analogous aspects of the implementation of this system versus some of the internals of of post Chris like the checkpoint system um one of the big decisions that we made as far as our design objective to uh make this system not invasive to post was to store the operation history uh inside of the power syn service middleware so we basically have a pluggable storage layer there um where we store the operation history and so effectively we're creating a rebuildable uh persistent cache outside of postgress and we keep the postgress um database clean so that we don't you know put a bunch of metadata and schema changes in the postris database we store the recent history of operations um for each row the reason why it's recent history is that we compact the operation history so we don't have an infinitely growing operation history um we represent the rows as Json in the middleware storage uh and then very importantly the operations are indexed by an operation ID which is a strictly incrementing integer ID which allows us to uh query an ordered list of operations very efficiently and then uh the users are authenticated using uh jwt's so I think the details of that are not as important so I'm kind of going to skip over that but once the users are authenticated um then the system does streaming sync of the bucket data from the parsing service to the clients so it basically figures out when a user is authenticated it figures out okay which buckets are applicable to this user and then it keeps a connection open and continuously streams any operations added to those buckets and then also it also takes into account changes in the bucket configuration like added and REM and removed buckets and on the client side that data is then persisted in sqlite so have a we have a type mapping from the postest types to the sqlite types of course sqlite has a very very limited type system only a handful of types and the replicated data is stored in a schema less format in sqlite so um the application does then Define a client side schema with tables columns and indices which is of course based on the postd schema and the sync rules and you can automatically generate that in that way um and then the schema is applied as sqlite views on top of the the schema list data and then the client SDK also provides uh live reactive query hooks like for example react hooks um to update the UI uh when data changes um so that's the read path so that explains how we do the dynamic paral replication from postgress to sqlite and then the next question is okay what does the right path look like if you because it's active active replication if you make changes on the SQL Light Side how do those changes make it back to postgress and as I mentioned um we send those rights through develop through the developer's existing backend um so the developer can just write to the local sqi database um but then we also the client SDK automatically puts those rights onto an upload queue and then the developer has to define a function for upload those changes to their back end and from there um writing the changes to post so the developer geke keeps the writing to postas they don't have to care about retrying and network failure logic and the client SDK does take care of that um yeah so that's the the right path and then as soon as you talk about merging changes then the the big question becomes okay how are you merging changes what about conflicts and that's where you know crdts typically come in and um one of the interesting implementation aspects is that we decided not to implement crdts on on a protocol level so if you're not familiar with cdt's Conflict Free replicated data types um they're essentially a special data structure where changes can be merged in any order and um uh the conflicts are always resolved in a deterministic way regardless of the order in which you merge them so that the replica all the replicas then converge to the same state an alternative to crdts broadly speaking is that if you you can always merge changes in the same order and then all the replicas converge to the same state so our system uses an architecture where there is an authoritative source of Truth regarding the global order of oper operations which is maintained in the middleware paring service um that is derived from The postgress Logical replication stream so our system does not actually use crdts on a protocol level so that has some upside and downside the downside is that you don't necessarily get the sort of predefined deterministic behavior of how conflicts are resolved um but the upside is that it provides um Simplicity and customizability for the developer so um the developers back end can reject and accept changes as needed and then all the clients will converge um to this to the server authoritative State um and then the other part of the flexibility and customizability is that the developer can also apply a custom conflict resolution and that can actually include using crdt data structure so even though we don't use crdts on a protocol level uh you can use um crdt data structure stored as blob data in postris so we actually have done a PC where we take the yjs crdt uh for Rich Text editing and Store The Blob data in postgress and then um guaranteeing consistent so we keep track of right checkpoints on the server so checkpoints have an Associated operation ID and post LSN um as I mentioned the local client side rights the developer can just write to the sqlite database those rights are applied on top of the last checkpoint receipt from the server so that if the um app is offline and the the app queries the local sqi database those query results reflect local mutations that have not yet been uploaded but then they're also placed into an upload queue and then the client retrieves the latest checkpoint from the server after it has finished uploading all the rights so this upload queue is empty and those changes have been written to post Grace then the client uh updates its local state to match the server State and then that way you can see how all the clients basically converge to the uh server authoritative State and in data Integrity um you know what happens if the data gets corrupted that's pretty simple we just basically compete compute a per bucket check sum on the client and server side and if there's a check sum mismatch then we redownload uh the bucket so tying it all together um you can see how this enables kind of a active active replication between sqlite and postgress in a way that um is kind of not invasive to postgress it's sort of you can sort of plug it in and easily remove it again and that enables that kind of local first architecture so um logical replication is of course a capability and postrace that um has gone through a lot of improvements over the last several releases and it's a really powerful capability it has enabled a lot of really interesting Solutions um in the Eos in the postrace ecosystem uh it's super super useful and of course of course we wouldn't have been able to build this uh without logical replication um so it's a critical capability uh we did run into some challenges with it so um there were some challenges where we sort of just had to make some tweaks as far as adapting it to the parsing model or architecture and um those are sort of these four that I'll that I'll just walk through so one of them is handling schema or ddl changes so um as of the current version of postgress um ddl changes do not get published to The Logical replication stream this may change in one of the future versions of post there is some type identifier information that is published where you can identify the column types and names so our objective was to make the system sort of low maintenance um including regarding schema changes so there's a few things that we did right off the bat in pursuit of that design objective and this is one of the big reasons why we made the powering protocol and client side database storage schema list and then we apply seite views on top of the schema list uh data um so schema migrations hardly affect the client side database and then also sync rules can apply some transformations to the postgis data during replication so um this means that the developer can use that for backwards compatibility so they can allow um the clients to keep using an older schema even if the postest schema has actually changed so that reduces the surface area um that's affected by schema changes in the system U but of course schema changes do still affect the system especially on the replication side there is still some surface area so our approach was basically um to try and detect wherever possible and either automatically handle or document a manual procedure um for handling uh schema changes so this is the list of sort of seven um schema change scenarios that that we currently handle you can see four of the four out of the seven are detected automatically whereas three of them cannot be detected automatically and require developer intervention and developer action and in two of the four that are detected automatically um we do resync the entire table so for example if the replica identity changes we uh resync the entire table um there are some fundamental limitations as far as what we can do with the postr schema for example generated stored values are not published in The Logical replication stream uh but generally this uh architecture works works well and kind of provides predictable behavior for the majority of cases the other challenge that we ran into was that the log sequence numbers in post um we needed a mon monotonically increasing operation ID sequence and initially we actually derived that from the postrace lsns but we moved away from to a separate Auto incrementing integer sequence uh for a few reasons one of the reasons was that the lsns uh in the postris right ahead log are not actually guaranteed to be monotonically increasing so transaction commits are always in sequence in the right head log but operations inside each transaction can have LSN ranges that overlap with other transactions um so that was one of the reasons the other reason was uh we wanted an architecture that can support sharded data bases where each postgress instance has its own LSN sequence that has to be merged into a singular op ID sequence so basically multiplexing and then the last one was we need the ability to reprocess sync data and there then we need to continue the existing op ID sequence while the LSN sequence would basically uh still say the same um the third thing that we had to solve was toasted values so uh toast is the oversiz attribute storage techniques where basically values are stored in a separate table if they exceed the page size uh and then in The Logical replication stream um The Toasted values are only published if the value has changed um there is one exception to that which is when you use replica identity full then the full value of the toast is the toasted attributes are always published in the keys field on The Logical replication stream uh but we obviously can't guarantee that postgis databases are necessarily configured that way and it's also less efficient so in order for simplic for Simplicity sake we wanted to design the system in such a way that it can always access the current field value of any updated field coming across the logical replication stream regardless of toast so what we did there was to store a additional copy of the current value of any field that we replicate which um is a form of stateful stream processing which ended up very working very well uh to handle that scenario and then the last thing was handling different row identifier permutations so post has options in terms of row identifier information that can be recorded in The Logical replication stream through replica identity uh so you may be familiar with them default full using index and nothing um default is is obviously the easiest we just use the primary key to identify um a a row so that when an update or delete to that row comes through we can figure out you know what update or delete what roow does that operation refer to uh but then there's also other settings um like full and using index uh full can be used if your table doesn't have uh any uh primary key for example but you can use it in any in uh you can use it you know regardless um and then there we basically just generate an ID from the relevant columns that are included but there's also a setting called nothing where where no information is recorded in logical application stream and there we generate a random ID if you have logical replication enabled postgress will block updating or deleting rows in that case but we still need a random ID to keep track of the the replicated row so that if somebody if the user if the developer truncates the table then we need to figure out which um which rows need to be truncated so um yeah that's uh basically the the conclusion here um we want to really want to help Advance the local first architecture adoption um local first has several uh benefits for both developers and end users and um we're excited about the role that the sync engine can play in um the local first uh sort of uh uh emergent architecture and U with this architecture we can provide sort of postgress to anything uh by directional sync currently focused on SQL light and um we're excited to see where this can go so if you want to learn more about the sync engine um you can go to par sync.com and um yeah I'm also happy to answer any questions [Music]
