# Transcript: https://www.youtube.com/watch?v=2FB-Yrf8eLo
# Source: youtube-captions
So, my name's Arushi and I'm a software engineer at Figma. Today, I'm going to be talking about how Figma enables real-time collaboration through our custom sync engines. So, Figma has not one but two sync engines. They're called multiplayer and life graph. For the last few years, we've grown a lot. So, I'm going to be covering some of the scaling challenges we've had. And I've specifically worked on Live Graph, one of our two sync engines. So, there's a couple of lessons about Sync that we've learned along the way. Um, but first, for those who don't know, Figma is a platform for teams to build products together. Our flagship product is for design and it's designed to be fully online. So we have very limited support for offline capabilities. Collaboration is front and center because we believe the best products are built together and we try to make that as effortless as possible for teams. So our design platform was the first design tool built for the web and it set a new standard for building on the web. Um our core innovation was multiplayer like Jan said and it was a big value ad and it showed that our bet really paid off. So multiplayer is inspired by conflict-free replicated data types. It's not a true CRDT in and of itself because centralization is faster. I forget who said that earlier, but it allows us to have a faster and leaner implementation and also handle conflicts more easily like when multiple clients are editing at the same time. Evan Wallace, one of the co-founders of Figma, has an excellent blog post on this and I would definitely recommend it to anyone who wants to dive deeper into how Figma did this. However, Figma has other product services as well. So, one example of this is comments. Comments appear in the canvas, but they're not built into the document type. They also appear in other product surfaces such as the sidebar. We have services like library publishing. So library publishing is one of the most powerful features in Figma and it allows designers to make reusable components and styles in a library file which then is queriable and consumable by other files. So a designer can make a button and then you can go use that in all your Figma designs. So this type of data is derived from a file but it's not it's not pulled into other files through fetching that document itself. And then Figma as a product has many other services that are outside of the multiplayer canvas fully. So we have file browser for example. All of these services still need to have real time. So even in file browser, we show active file users, anyone who's editing a file at the same time. So you could be asking, do you really need two sync engines? If we already had multiplayer, which sync CRDT like data, did we need to go build another one? Can we not kind of like format our data into this multiplayer model and reuse it? Um, so there's a few reasons. Not everything can be a CRDT. So one example, active file users. This is kind of ephemeral state. It's state that lasts for the duration of a user session and it would be wasteful to kind of persist that on the back end or build it into this document that's being synced with the server each time. There is also library publishing which I said. So when a designer for example makes like a button in a library file that's then pushed out to other files, you don't really want them to be editing that button in the library and everyone who's using it to kind of see that data being updated in real time. Instead we want the designer to be able to say like I'm publishing the button now and then all the other files pull in that snapshot. So this is kind of like immutable derived state from the file document. And then there's also a lot of data that's fully driven by the server. One example is notifications, our notification bell. And these don't really fit into the CRDT model. And it doesn't really make sense to force that data into that state. Um, but these features all still need to be live updating. So when we're thinking about this new sync engine we wanted to build for Figma, it was really about giving product engineers the ability to easily make the new features they're building live updating without any additional work on their part. Um so this was kind of our thesis around live graph. Product engineers should be able to focus on feature development not data sync and especially not bespoke data sync for each of their use cases. So, life graphs look something like this. Um, and I hope you're familiar because the convex talk had a really similar API. Um, but we have a use subscription react hook that automatically rerenders React components when the underlying data has been changed. And I just wanted to give one example of how multiplayer and live graph kind of work together to keep Figma real time. Um, so this example is thumbnails. In Figma, we show a thumbnail image of a file in the file browser. This image updates as the file is being edited and you can set a thumbnail image as well. So what the model for thumbnail looks like is kind of like this. We have clients who are editing a file that goes off through multiplayer which is synced to our fig file storage backend. Live graph is tailing database updates, getting updates on this thumbnail image, and this is kind of read back down to other clients that are looking at the file browser. Cool. So, a quick recap. Here's the differences between multiplayer and live graph. Multiplayer syncs the raw Figma file data while live syncs data from non-file sources in the Figma back end. So, Postgress and Reddus are some examples. Multiplayer handles both reads and writes over a websocket session to our multiplayer server while life graph is a readonly system. So all rights go through our REST API legacy back end. We do have optimistic updates to give product engineers that ability to um more easily sur like surface overrites but yeah live graph does not handle any rights and then multiplayer like I said is inspired by conflict free replicated data types while live graph is actually inspired by graphql um really kind of focused on this idea of ergonomics for product engineers but with the key addition of live updates because that was a mustave for Okay, cool. So, yeah, I wanted to share a couple of lessons from scaling sync at Figma. When I first joined, we supported a few use cases on life graph. It was like comments and design systems. Um, and we had I think like several hundred thousand users every day at peak. Now that number is into the millions and live is a P 0 critical service at Figma. So if life graph go down goes down the site is down and this was like a huge shift from where we started. Okay. So not all sync engines are created equal. And I really enjoyed this morning seeing like the variety of sync engines that we've gotten to learn about. Um but a lot of times when we talk about sync engines, we're looking at a model that looks like this. you have some data on the server and you're trying to keep that exact data on the client in sync with each other. Um, this is a model that we use for multiplayer. It's one Figma document that the client reads and the server persists and we're trying to keep that in sync. But when we talk about live graphs, that's not the model we have. We have some data on the server that can be written to by a lot of clients and we have a whole bunch of transforms happening in the middle. So what clients see is not what's persisted on the back end at all. So this caused a lot of pain for us in scaling live graph and this idea that you can like transform data and keep it keep having it live was what some people say the original sin of live graph. And it's because scaling multiplayer was relatively proportional to scaling. You scale up your servers at the same pace that we're growing the number of users on a Figma file. For live graph, that was not the case at all. And our growth in traffic is actually really driven by us handling these computations which can get really big, can do arbitrary business logic in them, and can potentially like refetch more data underneath. Um, and this also poses a problem for us when it comes to thinking about correctness of sync. So for example, if you have a query that's maybe fetched in some view and some computation and those are both watched by the same client, ideally if that query gets an update, you show that change to the client at the same time in both like the normal data case and the computation. So, for example, if you had like comments on the canvas and then comment count off to the side, that's really easy to do client side. But if you were going to put that in a computation, you want those things to always stay in sync. Um, so with computations, it's it's hard to do this. They take time to run. You can go fetch off more data, and it's really hard to deliver strong consistency under this model. Cool. So beyond the architectural and kind of like use case differences with multiplayer and live graph, we also had a lot of scaling constraints that were different. So multiplayer sync is limited to the file itself while live is like any data on the Figma back end and can allow arbitrary computations over that. And then multiplayer is also limited to a 100 clients of the same file while live graph is very different. We have data on the back end. you can be read by any number of sessions. Um, cool. Yeah. So, in scaling live graphs, another lesson we learned is that things get complicated really easily. Okay. So, back to this model. This is like kind of getting towards where we deal with things that we deal with in live graph, but it's like not quite complete. In live graph, we have many clients. So, the model looks something like this. And thanks Figma designers for these lovely illustrations I didn't make. Um but this is still not complete because at our scale you can't serve data directly from the database. You need to throw a cache in between it. So yeah this is a difficult problem and live graph's architecture today looks something like this. We have our primary server with a query cache in it. It's building views over our GraphQL like object graph. This also manages permissions and runs computed fields and we tail the Postgress replication stream for updates. These updates then get propagated through the server down to the client over websocket which maintains a local object store. So it's a stateful client. Um yeah so it actually looks really simple on the slide but it a lot of times feels like we're trying to build our own database working on live graph because we have a index for maintaining which queries are updated depending on like which database mutations we have some form of transaction. So for example, upon a database update, we try to send all the query updates out in one batch um and we tell the replication screen just like actual database replicas. Um which leads me to my third lesson, which is that scale directly informs design. Seems very obvious when I say it, but there's a couple of interesting examples of how this played out in Live Graph's evolution. So when live was first created, we were running all of Figma on a single Postgress instance. It was the biggest Postgress instance that Amazon RDS offered. But it was still one database and it was constantly at risk of toppling over. So for this reason, we decided on an architecture that never reququeries basically upon an update in Liverraph. Instead we send a minimal diff at each layer live graph to get to update the client state. That architecture looks something like this. So the database sends down row mutations that the query cache mutates its in-memory state from sends out that sends out query mutations to our server our main live graph viewbuilding server. And we have an isomorphic client and server. So that allowed us to iterate really really quickly because there was a lot of shared code between client and server. Um and it also allows the client to kind of process these similar minimal diffs that the server is processing. It's a very stateful and mutationbased architecture. So we have this stateful query cache, stateful server and stateful client. And this is really hard to scale as we've realized. Um, for example, if you drop in the middle of a stream, say in the server, you're you can't be sure like is it safe to replay updates or not. Um, a lot of times these mutations can't be reordered. There will be correctness issues if you reorder them. Um, yeah. So, it's very and when you when one of like a server goes down, you have to refill that whole state, which could cause a thundering hurt on the database. So, very hard to scale. Um, but yeah, this isn't the world that Figma lives in now. Today, we've sharted our databases both vertically and horizontally and we have replicas for each primary instance. So, we're slowly moving towards this world where we have a stateless invalidation based architecture. So, the database can send query invalidations to our query cache which sends view invalidations to our server view cache and we rebuild in order to get the new state to send down to the client. This breaks the isomorphism between the server and client. Um, but it allows us to have a stateful client and the data on the back end with a bunch of stateless layers in between. So we can really easily scale out the layer that we need to scale out. Cool. Okay. Another interesting thing that's happened as we scaled is this question that's coming up like do we even need live graph? So as the number of Figma users has increased, the percent of traffic with live updating data has decreased. And even though the volume has increased, it's like the percent of data that's being sent down to the client as a result of other people's active changes. That's like what's gone down. Um, and this is due to a few reasons. There's more Figma users, so more people who are not designers and maybe they're just viewing a file. But also, Live Graph was really built for this case of being ergonomic for product engineers to use. And because that happened, product engineers started using live graph off the cuff for a lot of features they're developing, not necessarily those that actually need to be live updating product surfaces. Um, so we've started to think about a lot of different problems that we weren't thinking about at all when I first joined. And one of those is initial load prioritization. So when I first joined Figma, we were the stage that live was kind of at was like can we consistently serve comments without live graph server going down. Um like I said now Figma powers critical user flows such as the file browser. So it's a lot more important that in failure scenarios we can serve something rather than like being able to serve updates all the time. So one of the ideas that we've kind of been thinking about in this space is initial load prioritization where we do the initial view fetch over HTTP and then open a separate websocket subscription for updates. These two things do need to be like linearized and merged on the client. But it allows us to scale a lot easier because for example we have whale users, people who fetch like huge views at a time that can overload our server. With initial fetch over HTTP, you can spread that load out across many servers and allow the load on any server to be more predictable. It also gives us some other nice benefits in failure scenarios such as graceful degradation. In the case of an incident, you can choose to just kill the updates and still be able to serve limited functionality on Figma. Yeah. And I thought that was an interesting example because it's really only something we've been talking about in like the last few months. This is not anything that we were thinking about like a couple years ago. We were just trying to keep live updates working properly. So yeah, that's kind of the three lessons I have. I've had a lot of fun working on sync engines and I for I think James maybe said this like I love it and I hate it. That's kind of how I felt as well. Um but yeah, if you have any questions about more technical details, I know this was pretty high level so feel free to find me afterwards and thanks for listening. Thanks to my team. [applause] [applause] couple of questions >> anybody. >> All right. Can you speak more about thundering herd? Like if that has actually happened and you guys have kind of killed the herds. >> Yeah. Um, this is actually in my presentation the first time and I took it out, but we because we have so many stateful layers, when one thing goes down, you have to kind of re refresh all of that state, which causes a lot of load on the database. So, I'll pick an easy example. Our clients when they reconnect over websocket, they try to get all of that data again. Um so every day when we deploy all the clients disconnect and then they reconnect try to get all of their state again. Um in the beginning we actually didn't deal with this problem at all. We decided to just deploy very slowly. So it would take like two hours to deploy live graph. Um but one of the ideas that we've been working with recently is to reconnect using a cursor. So that does require like global ordering of your update stream, but you can have a cursor that goes all the way down to the client, for example. The client can send up the cursor on reconnect and then you only need to rebuild state that might have changed. >> Uh my question is, is two sync engines the right number of sync engines for Figma or should it be maybe a smaller number or a larger number or is that just what you kind of landed on in development? >> Yeah, that's a good question. for a time we had I don't want to say three sync engines but we had like two sync engines and something that was trying to do something similar. Um yeah so multiplayer it's all about like the specific use cases I think and multiplayer was like specifically for this canvas use case and live graph was very much for like the product engineering ergonomics and like let's not have anyone spend more time. I do think there's a lot of use cases that live has that probably could be like a simple event stream that the client's hooked up to and then you refetch. But because we built live inhouse and it integrates with things like permissions, it's really nice to just have that all of that like framework for free for product engineers. >> Yeah. >> Um Can you repeat the question? >> Yeah. What do I think about integrating real time tax editing into Figma like in the canvas? Um I do not work on multiplayer so I'm going to sidestep that question. Yeah. >> I question. Um how do you guys handle bad connections? is like do you guys fall back on something besides websockets like long pulling or what is the general approach you guys use for this particularly with like enterprise networks have these weird issues and whatnot? >> Yeah. Um we considered doing HTTP long pulling but we only have websocket right now. Um so yeah because the client is stateful like you can it doesn't fully break whenever the connection goes down. Like there's a couple things you can do but yeah we don't have any other backup. >> Got it. And then does is a live so like for the new Sigma slides product is that uh leveraging live graph as well? >> Um sorry I don't know where you are so I don't know where to look but um it's it's again both multiplayer and live graph. So the part of slides that is like multiplayer collaboration on a canvas like if you in slides you can toggle between design and the slides mode. Those are basically the same thing. It's the canvas which is multiplayer. The things around it are a lot of live graphs. >> Any other questions? >> Hey uh simple one. In which language did you implement beckins that tails flows present? >> Yeah. So multiplayer is implemented in rust and it used to be typescript. We moved to rust. It's completely rust now. Live graph is a mix of TypeScript and Golang. So originally it was written in TypeScript and we're like slowly moving things over to Golang. >> All right, let well wait one more. Okay, I was point blind spot. >> No worries. Hi. Um I know you talked about kind of uh how uh live started being used by product like people in ways that you originally didn't expect. How did you kind of design the API in a way such that like oh people like use it for the right thing and how do you think about that sort of like developer ergonomics and to kind of like lead them into the pit of success? >> Yeah. Um that so I feel like we were in a period of time where we were kind of just like use live graph we're going to support what you what you decide to use it for. We'll maybe build some stuff out for you. So for example we have live graph like the schema language is kind of like graphql but you can fetch from other backends not just postgress like reddus or you can fetch our rest api from it as well. Um and that made live super powerful. It was kind of like self-s served. We've kind of shifted more into a zone of like now we need product guard rails because it's a shared schema and um shared schemas are really hard to maintain. So there's like a lot of over data fetching or like queries that we don't want to allow happening. So yeah, we're starting to add like guardrails around our schema which the a lot of like GraphQL open source is really helpful for for like statically doing stuff on the front end.
