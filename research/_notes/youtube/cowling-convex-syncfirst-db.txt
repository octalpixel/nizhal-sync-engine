# Transcript: https://www.youtube.com/watch?v=ngN3PoXDFJI
# Source: youtube-captions
Cool. Hi everyone. I'm James. I have a lovehate relationship with distributed systems. Um, and I've devoted my whole life to distributed systems. I used to work in cellular and um satellite and Wi-Fi protocols. Then I did a PhD in consensus protocols and distributed transaction commit. And then at Dropbox, um, designed the multi-exabyte distributed storage system and the distributed database that runs several million consistent transactions per second and multihoming, all that kind of stuff. Work closely with Jamie who designed the sync protocol at Dropbox. Um, so I I guess I chose this life, but distributed systems are frustrating. And if you don't think distributed systems are frustrating, I challenge whether you work in distributed systems because they are hard. They um have very subtle, difficult, emergent properties that don't show up until way later on. And gonna drop some hot takes here that I stand behind strongly. Right? I don't have no desire to gatekeep. I want more distributed systems engineers. Uh and if you want to be one, please come work at Convex. But talent is plummeting in this area. There is just not a lot of deep distributed systems people on the job market. Everyone's thinking about building AI or building applications. I think that's great. Mostly people are focused on building value products that have value. And I don't think the robots are coming to save us, at least for a while, right? And again, if you think the LLMs are good at systems architecture, I just don't think you understand systems architecture yet, right? Because it's there's good reasons why LM are not good at this, right? Because a good distributed system is not one that just works. It's one that's simple and maintains that simplicity and that maintainability over a course of years as your requirements change as your workloads change and as it grows over time. I can't point you at a good um training set for distributed systems. I can't give you a good eval for distributed systems. And building good distributed systems means reasoning about unrelated components and how they interact in ways that don't mesh well with context windows. Right? So, I think there is this looming systems crisis largely for a good reason. People want to build applications, but they don't want to build like databases and and sync protocols and all that kind of stuff. Great. Someone has to solve that problem. That's why we're all here, right? Um, now I'm going to go down my um personal tangent of where how I think systems should be designed, right? And I think the the industry has trended away from the architecture that I tend to like. And I think in two ways, we've given up on transactions. I'll talk more about this. I think transactions are the most valuable building block for like concurrent systems. And we have this amazing wonderful thing called client side reactivity. We have React. Isn't it cool that we just think it's normal now that data changes on your client and your UI just rerenders consistently? You don't have to do that manually. But there's no relationship to the backend state. Right? So we have client side react. We we've made the easy problems or like the at least the the userfacing tractable problems very simple and we've made the backend problems even harder. Right? So I think in many respects we've created maximal race conditions. Right? We've um taken away consistency. We've added all this power. We haven't tied that to the data that lives on the server. The important stuff. Um, so in my mind, the holy grail of full stack development, the primitive that developers need, the modern application developer needs is automatic end-to-end consistency from the the server to to the client. And this is what I'm calling sync. We're going to have a panel session later on. There's lots of definitions of sync, right? But my this is my version of sync. Um, certainly doesn't preclude other definitions of this, but I think to me sync means keeping data on your client in sync with the data on the server. This is an example of what this looks like in convex. This is just me messing with um state in the dashboard on convex and this is a little demo app on a cell phone. Um this is just going over the internet, you know, and it's happening instantaneously. And when you update state on the server, the client updates in a consistent way. Um I'm not talking about local first. I think there's a lot of really interesting local first ideas out there. We haven't quite nailed it yet. We have two working prototypes of of local first in comb with full offline support and everything just haven't quite nailed it yet. Um I'm going to focus on uh what platforms need to look like what the building blocks need to look like to make race conditions go away. So I think this is that the operating system for application development. I think this is the three things my candidate three things you need to build really good applications. One is transactions. Now I don't think I don't I hope I don't have to explain to this audience what a transaction is and why a transaction is interesting but I will anyway right transaction you call begin you do a bunch of stuff you read some data you make some decisions you write some data and then you call commit and it happens all at once or not at all that's the A in acid and it's the I in acid but it's um the point of a transaction is that when I'm writing some code to add a new user to the system and I have to go add a project for that user and I have to go set it quotota and I have to look up here and decrement the available count and change their billing. I don't have to reason about what happens at every step of that code if someone else intercedes and changes it underneath me. Right? This is basic stuff. This is thread safe programming, right? Transactions so important, right? But again, I I guess this is a hot take. I don't know why it is, right? SQL is not an expressive language, right? Almost no one writes their business language in SQL because you can't have an if statement in SQL. You can't run code, arbitrary code in SQL. At le you could use PL/SQL like store procedures. No one wants to do that. Um, typically you write stuff like this. This is just some Python OM code that runs in a transaction and it reads something from the database and it checks an if statement and it raises an exception and does a conditional check. This is how people have built database code for the past 50 years. Um, cool. So, I don't have a problem with the old way of doing things really. This is how like every big web service you use looks, right? You have your client on the web browser. You have a RPC server running code that looks like that Python code. It talks to a database. It runs transactions. But there are some challenges here. There's two big glaring challenges. One is the obvious one for sync comp. There's no sync here. You have to keep pulling the server and updating your data on the client. That's actually very hard for application developers. The second one, which you might not think about unless you've worked at a big company, right, is the API servers written by the API team. and they're like grumpy people and you got to ask them to do stuff for you. And this is not a little point. This is this is a real issue because the world is now owned by the application developer and the application developer wants to write their code in in their front-end language in Typescript and have it in their monor repo, right? And so there's another option out there um which is um quite popular especially in in uh platforms like um Firebase or Superbase etc. right? Where you put the client business logic on the client. You're like, "Hey, we're our target market is front end developers. Put your business logic on the client. Send SQL statements to the server." A few issues here. One, you have to have this complicated roadle security rules to lock down your state so you don't leak it. Um, the other one, the big one for me is you're giving up on transactions probably because you can't easily issue a transaction from a client to a server. can't let your phone or laptop call begin transaction on google.com, right? And then I go away and I've locked up a bunch of server resources. So this is kind of given up on transaction. So I think the the kernel the new abstraction for application development probably looks like TypeScript stored procedures running on a server and and reactivity to solve the sync problem. So, I like the old school model, but I think we should kind of amend amend it a little bit. So, instead of having the API team write the API services in ways that are kind of unsafe, so a different team writes it, um, let's just have people write TypeScript store procedures written in TypeScript using the same code as your front end. They got automatically synced to the server, run safely in a sandbox, and that runs transactions. and let's add a sync protocol on the client to automatically keep the client up to date with respect to data on the service. How it looks in practice, you've seen this um this is a I've been reusing this example for several years, so I apologize for this, right? But this is a really simple shopping cart example and you have two major components here. On the left, we have items in stock and on the right we have my private shopping cart and the left is shared. So if you were on this website and I was adding things to my cart, you would see this stock available for purchase go down automatically and I would see it add to my cart. And this is a simple application. It's actually very hard to implement in a traditional way because of all the weird and interesting relationships between all the components and all the operations you might do. You got to think about what happened with this card and that user and what happens if someone changes it while I'm adding it over here. Right? In convex if we have these primitives things are very easy. So this is the code the actual code for fetching the data on the left side of the screen. This just says query all the items where there's remaining stock and return them. There is a transaction. That's not a very interesting transaction. Here's the transaction to add an item to my cart. This is this needs to be a transaction, right? This checks if above the screen it checks if I'm authorized to do what I want to do and then it checks if the item still exists and then it throws an exception if if there's no stock left. Um and then it adds an item to my my cart and then it decrements the available stock. Right? It's a mutation but all it does is reason about moving an item from the you know the the items table to my cart. That's all it does. It doesn't think about any race conditions. It looks like this. So, how do we bind this to the client? How do we make the client update? Like polling and consistency are hard problems, right? This again, this is the real code. This is on the client. Now, this is the real code for rendering the items component on the left. It says take the get items query, bind it using a react hook to the items component to the items uh you know value and then just render a div. That's it. And because it's React but for the back end, anytime data on the server changes, it automatically rerenders the client in a consistent way. Right? So this to me is, you know, transactional sync. This is the primitive we want to provide. Um, so what do we need? I'll get into some details now. What do we need to build transactional sync? We need a runtime to run your code. We need a database, a commit protocol. And then we need to solve some of the traditionally hardest problems in in computer science, right? Consistent caching and and and endto-end consistency. So the convict runtime has queries and has mutations. Queries are read only. Mutations change start data. They're arbitrary Typescript. We make sure they're side effect free and deterministic um by remapping random and time etc. and they can call onto the database. So conceptually it looks like this. You have the database inside of which you have V8 running isolates and your code runs inside the database. You can just write regular code calling regular functions. It can be thousands of lines long. You can call other functions but it runs transactionally inside the database. In practice, it looks like this. Actually, in practice, it looks like this. Um, I don't know if there's time. We can talk about this or have other talks about how it all fits together. So, I want to talk about how you build this thing. And I'll talk about it really fast. Um, how would you build um a transaction protocol looks like this? And why is convex a database and not just a sync protocol? So we've at the bottom of the stack in this diagram here if you can see up the top right here there's Aurora or planet scale it's a versioned write ahead log with some convenient indexes right and then we have this service here called funr run fun runs the function runner all the cheesy names are mostly my fault um and function runner runs user code right so when you kick off a transaction and it says hey um move that item from this the items table to the card etc. It reads a snapshot of data from the database and it's designed with indexes so you can efficiently read as of time stamp t give me a consistent snapshot of the database. Then we run the code run the code on that state as of the snapshot. And while we're running that code we keep track of all the data ranges that query touches. So it reads a bunch of items and maybe reads some user state in in a read some cart state and we know all the data ranges that that have um have been read. We buffer off all the rights and at the end if no other transaction has in has their rights interfering with any of our reads. We commit the transaction otherwise we retry it. Um there's more detail here. I'm going to skip over that. This is classic multi- version concurrency control. This is not like a crazy thing we've invented. This is a traditional way of um building optimistic concurrency control databases. Our protocol is a little bit similar to say foundation DB for example. But there's some really key observations here. One observation is that for every query that we run, we know the input ranges that query depended on. And we also know the outputs to that query are a deterministic function of the code that we ran and those inputs. So we just pause and think about that for a second like what can we do with those guarantees? What can we do with with those features? Right? We can do some really interesting things with those features, right? One is that caching comes for free, right? Sometimes people ask um how do I cache things in convex? I'm like you don't. We'll just do it for you. Right? we have a consistent query cache um server side query cache there's other functionality for client side caches I won't talk about today we know whenever the result of a query changes course we know all the rights that are happening in the system so we know the the results of this query that depend on the inputs are valid until those inputs change right so we just cach it for you forever until those results change right um it's a query cache a Query cache is a much more powerful thing than a row cache. In my opinion, row caches cache the data that gets written from the database. A query cache caches the result of a transaction. It saves you running all this work. Again, so a very large fraction of the work in convex is just served out of a query cache. It saves us a lot of money and it saves the user a lot of latency. The caching is hard. This is what I I mean I cared really deeply about architectural simplicity and um composable abstractions, things that fit together well. And you know when a abstraction is not composable, when emerging complexity starts arising, caching is hard. I'll give you an example of why caching could be very difficult. when you cache the results of a query that's predicated on the source code for that query and if I go and upload a new version of my mutation that needs to invalidate the query and if you write your code such that you keep manually adding all these corner cases and invalidating stuff right you end up with a very complicated protocol in in convex we just store the source code in the database conceptually at least right and so the source codes and the read set for the query free, right? So if you go and upload new source code, that just conflicts with a query and we reinvalidate and rerun it, right? Great. Reactivity comes for free. So if you have caching and you know what the clients are watching on their screen, you can just send send them the new value whenever it changes, right? So we have automatic endto-end reactivity, right? And this in in um my pants at least in the vocabulary we use the Dropbox is a sync protocol. It's very similar to what we um you know how this file sync protocol works, right? You have a websocket from the client connected to the server. The server knows all the use query hooks that are sitting on the client. It knows all the data you care about and it can update that data and send it to you over the web socket to rerender your UI whenever the server state changes. So that's how we can do this thing here, this use query thing. This could be calling a relatively complicated query and we say, hey, anytime the data changes, just rerender this component. And that's making um that's making this app rerender, right? There's no polling here. There's no the component on the left doesn't know that the component on the right exists, right? It's just independent components showing a consistent state of of the world. Now, this is actually true end to end consistency. Now, one thing that happens in a lot of applications is you have a lot of different engineers writing a lot of different functionality and they don't want to think about what everyone else is doing, right? So, there's only two components on this screen. If you go to like facebook.com, there's one bajillion components on that screen, right? Um, and what's very undesirable is if an item shows up in my cart, but it's still available for purchase, right? or it's very undesirable if I join Slack and um I can send a message but I'm not available on the user pane or I add a task to a to-do app and the task shows up before the project that the task is in shows up right because that's when you start adding all this very complicated logic on the client to track these dependencies and the whole point of this is to make race conditions go away right well in convex we just know what you're subscribed to right so we just make sure that the client's always seeing a consistent snapshot of service state. The mutations go up over the websocket protocol with timestamps. The updates come down over the websocket protocol and you will only ever see a state of the world that's consistent with some data on the server. So this is what I think the modern building blocks of app development are. Now I think I hope we're right. I think we're right. I hope we're right. But I do very strongly believe that someone is going to come along with a new abstraction for application development. I think application developers are hungry for something better than just a you know glorified AWS right we need a higher level primitive that makes systems problems go away and I think the biggest systems problem is distributed data sync someone's going to do it um and I think and the convex pitch is that the two primitives we need a TypeScript stored procedures lets you run transactions it's written in the same language um that your client has written probably and it's it's expressive and also end to end reactivity so you don't have to deal with all that polling logic and reasoning about interactions between different components on the screen and that's >> [applause] [music]
