# Open questions — Nizhal sync engine (delegatable)

| ID | Question | Would resolve if | Suggested command |
|----|----------|------------------|-------------------|
| O1 | Should durable-offline `lww` updates be HLC-tiebroken instead of commit-ordered? (Code reality: commit-order, HLC ignored by lww — `server/index.ts:1021-1026`.) | A failing-first test "old offline lww update vs newer online update to same field" is written and product intent decided; optionally make lww HLC-tiebroken. | `/diagnose` then `/rfc-writer` (semantic change to merge policy) |
| O2 | Does revoking one of two overlapping buckets wrongly evict rows still visible via the other? (Code reality: `removedBuckets` is key set-diff with no row-level guard — `storage.ts:850-874`, `sync.ts:524-537`; the tombstone path *does* guard via `getVisibleRemovalRows`.) | A two-overlapping-buckets test revokes one and asserts other-bucket rows survive + truly-removed rows vanish (expected to fail today for multi-bucket-per-collection / no-`bucketField`). | `/triage-issue` (likely real correctness gap) → `/tdd` |
| O3 | Can a `related` data query widen read visibility beyond the actor's buckets at runtime? (Code reality: flattened to independent bucket-scoped queries, no correlated JOIN — `sync-rules.ts:185,223`; but `.related(` is unused in any Nizhal app.) | A VAPT test builds a `related` query whose naive join would pull a foreign-bucket row and asserts exclusion; plus an app that actually uses `related`. | `/delegate` security worker with `security-vapt` framing |
| O4 | Is `_nizhal_next_row_version()`'s singleton `FOR UPDATE` a write-throughput ceiling? (`storage.ts:1049-1058`.) | A concurrent-writer bench measures version-issuance contention vs throughput. | `/delegate` perf worker (bench harness) |
| O5 | What happens on crdt CAS exhaustion (silent drop, `index.ts:1110`) and does the crdt bytea grow unbounded (no compaction)? | A 6+ concurrent-writer crdt test + a byte-growth assertion over many edits; scope a compaction primitive. | `/diagnose` then `/rfc-writer` |
| O6 | Can realtime carry an incremental membership-diff instead of full repull? (Server already knows affected rows in `mergeAwareTx`.) | A "delta poke" prototype computes changed-row ids from the mutation and ships them on the `repull` hint. | `/rfc-writer` (new primitive, P1) |
